/* 在不使用运算符&或一的条件下编写一个与上面的for循环语句等价的循*/
#include <stdio.h>
#include <string.h>
int main(){
	int i = 0 ;
	while{
	if (i < lim - 1){
		if (c = getchar()) != '\n' ){
			if (c != EOF){
				s[i]=c;
				++i;
			}
		}
	}
	}	

//	for (i = 0; i < lim-1 && (c = getchar()) != '\n' && c != EOF; ++i)
//	s[i] = c;
	
	
	printf("%d",i);
	return 0;
}
