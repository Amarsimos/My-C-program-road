// 练习1-5. 修改温度转换程序，以相反的顺序打印温度对应表，即从300oF 到0oF。

#include <stdio.h>

main()
{
    int f;
    printf("华氏温度一摄氏温度对照表\n");
    printf("-----------\n");
    for (f = 300; f >= 0; f = f - 10)//从300递减，步长为10，到0停止
        printf("%3d|%6.1f\n", f, (5.0 / 9.0) * (f - 32));
}
