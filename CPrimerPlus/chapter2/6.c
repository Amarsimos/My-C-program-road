#include <stdio.h>

int main()
{
    int tose = 10;
    int twice = tose * 2;
    int power = tose * tose;
    printf("tose = %d\n", tose);
    printf("twice = %d\n", twice);
    printf("power = %d\n", power);
    return 0;
}